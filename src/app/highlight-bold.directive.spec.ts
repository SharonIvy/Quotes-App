import { HighlightBoldDirective } from './highlight-bold.directive';

describe('HighlightBoldDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightBoldDirective();
    expect(directive).toBeTruthy();
  });
});
